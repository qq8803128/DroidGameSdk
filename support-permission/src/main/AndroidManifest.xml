<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="droid.game.permission">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application>

        <activity android:name="droid.game.permission.PermissionActivity"
                  android:windowSoftInputMode="adjustNothing"
                  android:launchMode="standard"
                  android:configChanges="keyboardHidden|orientation|screenSize|screenLayout"
                  android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen"
                  android:screenOrientation="behind"/>

<!--
        <provider android:authorities="${applicationId}.permissionFileProvider"
                  android:name="droid.game.permission.provider.FileProvider"
                  android:grantUriPermissions="true"
                  android:exported="false">

            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/droid_game_permission_file_paths" />

        </provider>
-->

    </application>
</manifest>
